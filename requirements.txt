# AILEE Trust Layer — Python dependencies
# Pin major versions for reproducible Render builds.

# Web framework
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.29.0,<1.0

# Request validation
pydantic>=2.0,<3.0

# AI providers (optional — only the one you configure needs a valid API key)
openai>=1.30.0,<2.0
google-generativeai>=0.7.0,<1.0

# AILEE dev extras
pytest>=7.0
